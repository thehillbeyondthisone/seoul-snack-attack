# Project instructions

These instructions are persistent for this project and should be followed by
Codex in future tasks.

## User defaults

When the user provides working preferences or project settings during this
project, treat them as defaults for later tasks. When the user says the work is
finished, update this section with the final agreed settings, preserving the
existing project instructions unless the user explicitly replaces them.

<!-- USER_DEFAULTS_START -->
No final user defaults have been recorded yet. Keep the settings provided in
the active task in effect until the user confirms the final defaults.
<!-- USER_DEFAULTS_END -->

## Project continuity

- Read `README.md` and `handoff.md` when the task concerns the game, its build,
  assets, physics, rendering, or validation workflow.
- Preserve existing user changes and avoid destructive repository operations
  unless explicitly requested.
- Run the narrowest relevant validation after changes and report what was run.
